# Railway Configuration for Frontend
# Root Directory should be set to "frontend" in Railway dashboard

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
